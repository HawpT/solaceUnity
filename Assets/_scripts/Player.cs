using UnityEngine;

public class Player : MonoBehaviour {

    //this is reall all the player does right now, hold these two targets
    public Unit CurrentTarget { get; set; }
    public Tile CurrentMouseOver { get; set; }
    
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	    
	}
	
}
